*** Release-Hinweise ***

Dies ist diva-r09210 entsprechend OpenSim 0.9.2.1. Dies 
ist eine Hauptversion von OpenSim.

** Änderungen in OpenSim seit der letzten Version

- Siehe http://opensimulator.org/wiki/0.9.2.1_Release

HINWEIS: Die Standard-Skript-Engine von OpenSim ist jetzt YEngine

Wenn Sie von 0.8.1.* aktualisieren, sehen Sie bitte die Release-Hinweise unten für r08200

** Änderungen in D2-Konfigurationen

- Keine

** Änderungen in Wifi

- Keine


===================== Ältere Release-Hinweise ====================

==== r00911 ====

Dies ist diva-r09110 entsprechend OpenSim 0.9.1.1. Dies 
ist eine Hauptversion von OpenSim.

** Änderungen in OpenSim seit der letzten Version

- Siehe http://opensimulator.org/wiki/0.9.1.1_Release

Wenn Sie von 0.8.1.* aktualisieren, sehen Sie bitte die Release-Hinweise unten für r08200

** Änderungen in D2-Konfigurationen

- Keine

** Änderungen in Wifi

- Keine

==== r00910 ====

Dies ist diva-r09100 entsprechend OpenSim 0.9.1.0. Dies 
ist eine Hauptversion von OpenSim.

** Änderungen in OpenSim seit der letzten Version

- Siehe http://opensimulator.org/wiki/0.9.1.0_Release

Wenn Sie von 0.8.1.* aktualisieren, sehen Sie bitte die Release-Hinweise unten für r08200

** Änderungen in D2-Konfigurationen

- Keine

** Änderungen in Wifi

- Keine


==== r00900 ====

Dies ist diva-r09000 entsprechend OpenSim 0.9.0.0. Dies 
ist eine Hauptversion von OpenSim.

** Änderungen in OpenSim seit der letzten Version

- Siehe http://opensimulator.org/wiki/0.9.0.0_Release

Wenn Sie von 0.8.1.* aktualisieren, sehen Sie bitte die Release-Hinweise unten für r08200

** Änderungen in D2-Konfigurationen

- Keine

** Änderungen in Wifi

- Keine

==== r08210 ====

Dies ist diva-r08210 entsprechend OpenSim 0.8.2.1. Dies 
ist eine Hauptversion von OpenSim.

** Änderungen in OpenSim seit der letzten Version

- Siehe http://opensimulator.org/wiki/0.8.2.1_Release

Wenn Sie von 0.8.1.* aktualisieren, sehen Sie bitte die Release-Hinweise unten für r08200



==== r08200 ====

Dies ist diva-r08200 entsprechend OpenSim 0.8.2.0. Dies 
ist eine Hauptversion von OpenSim.

** Änderungen in OpenSim seit der letzten Version

- Siehe http://opensimulator.org/wiki/0.8.2.0_Release

WICHTIG

- Bitte führen Sie diesen Befehl in Ihrer DB aus:
  update os_groups_roles set Powers ='351843720888318' where Powers='349644697632766'

** Änderungen in D2-Konfigurationen

- Keine

** Änderungen in Wifi

- Option zur Ignorierung der Zertifikatsverifikation hinzugefügt. 
  BypassCertificateVerification = true | false, standardmäßig false

- Portugiesisch-Brasilianische Übersetzung hinzugefügt (Dank an Melanie)

- Jetzt kann die Serverseite eine Frontend-Sprache durchsetzen, ohne auf die Browser-Einstellungen der Benutzer angewiesen zu sein.
  Siehe FrontendLanguage in ini

- UserDocsPath ist jetzt konfigurierbar

[Rest der Release-Hinweise fortgesetzt mit historischen Versionen...]